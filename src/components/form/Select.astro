---

const { name, id, size } = Astro.props
---
<select class:list={["w-full border-base px-7 font-bold tracking-tight text-primary transition-all duration-150 bg-caret bg-[length:10px_6px] bg-[position:right_24px_center] focus:border-indigo-500 focus:outline-0 focus:ring-0 hover:cursor-pointer dark:border-white/10 dark:bg-gray-800 dark:text-white dark:placeholder:text-gray-500/80 dark:focus:border-indigo-500 dark:bg-caret-white", {"h-[60px]": size == "md",  "h-[66px]": size != "md"}]} name={name} id={id}>
  <slot />
</select>
